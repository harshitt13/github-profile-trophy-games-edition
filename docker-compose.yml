services:
  deno-app:
    build: .
    volumes:
      - .:/app
    ports:
      - "80:8080"
    environment:
      - DENO_ENV=development
    env_file:
      - .env
    command: ["deno", "run", "--watch", "-A", "main.ts"]
